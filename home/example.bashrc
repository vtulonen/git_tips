# ---------------------------------------------------------

alias tip="sh ~/tools/tips.sh"

# Set TOTD to HTTP mode (alternative is local files mode) -----
# export TOTD_HTTP=1
#
sh ~/tools/tips.sh

# ---------------------------------------------------------
